{
  "name": "profile",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "updatelambda": "del profile.zip && tar -a -c -f profile.zip * && aws lambda update-function-code --function-name profile --zip-file fileb://profile.zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^1.35.6",
    "axios": "^0.27.2",
    "jsonwebtoken": "^8.5.1"
  }
}
